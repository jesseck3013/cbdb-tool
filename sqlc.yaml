version: "2"
sql:
  - engine: "sqlite"
    schema: "internal/query/schema.sql"
    queries: "internal/query/query.sql"

    gen:
      go:
        package: "repository"
        out: "internal/repository"
        emit_json_tags: true
        emit_pointers_for_null_types: true